function solution(s) {
    let arr = [];
    arr = s.split(" ");
    for(let i =0 ;i <arr.length; i++)
        {
            for(let j = 0; j < arr[i].length; j++)
                {
                    if(arr[i].split("")[j])
                }
        }
    return answer;
}